<h1>Inventory</h1>

<table>
  <tr>
    <th>Product</th>
    <th>Manufacturer</th>
    <th>Stock</th>
    <th>Price</th>
  </tr>
  <% @products.each do |product| %>
  <tr>
    <td><%= product.product_name() %></td>
    <td><%= product.manufacturer().manufacturer_name()%></td>
    <td class="<%= product.stock_check %>"><%= product.stock() %></td>
    <td>£<%= product.selling_price %></td>
  </tr>
  <% end %>
</table>

<h2>Other Manufacturers</h2>
<% @manufacturers.each do |manufacturer| %>
<p><%= manufacturer.manufacturer_name %></p>
<% end %>
